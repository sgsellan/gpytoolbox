[tool.cibuildwheel]
build-verbosity = "3"
[build-system]
requires = [
    "numpy>=1.22.4,<=2.3",
    "scipy>=1.7,<=1.16.2",
    "setuptools>=42",
    "wheel",
    "cmake>=3.16,<3.27",
    "packaging"
]

build-backend = "setuptools.build_meta"