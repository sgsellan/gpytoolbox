[tool.cibuildwheel]
build-verbosity = "3"
[build-system]
requires = [
    "numpy>=1.14.5,<2.2",
    "scipy>=1.5,<1.15",
    "setuptools>=42",
    "wheel",
    "cmake>=3.16",
    "packaging",
]

build-backend = "setuptools.build_meta"