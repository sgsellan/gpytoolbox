[tool.cibuildwheel]
build-verbosity = "3"
[build-system]
requires = [
    "numpy>=1.22.4,<2.3",
    "scipy>=1.7,<1.14",
    "setuptools>=42",
    "wheel",
    "cmake>=3.16,<4.1",
    "packaging"
]

build-backend = "setuptools.build_meta"