[tool.cibuildwheel]
build-verbosity = "3"
[build-system]
requires = [
    "numpy>=1.22.4,<=2.4.6",
    "scipy>=1.7,<=1.17.1",
    "setuptools>=42",
    "wheel",
    "cmake>=4.2,<4.4.2",
    "packaging"
]

build-backend = "setuptools.build_meta"