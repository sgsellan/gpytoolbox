[tool.cibuildwheel]
build-verbosity = "3"
[build-system]
requires-python = ">=3.10"
requires = [
    "numpy>=1.16.5,<2.3",
    "scipy>=1.6,<1.14",
    "setuptools>=42",
    "wheel",
    "cmake>=3.16",
    "packaging"
]

build-backend = "setuptools.build_meta"