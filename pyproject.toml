[tool.cibuildwheel]
build-verbosity = "3"
[build-system]
requires = [
    "numpy>=1.16.5,<2.3",
    "scipy>=1.6,<1.14",
    "setuptools>=42",
    "wheel",
    "cmake>=3.16,<3.27",
    "packaging"
]

build-backend = "setuptools.build_meta"